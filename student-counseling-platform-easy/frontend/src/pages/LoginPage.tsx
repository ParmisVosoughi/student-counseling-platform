import {FormEvent,useState} from 'react'
import {Navigate,useNavigate} from 'react-router-dom'
import {useAuth} from '../contexts/AuthContext'
import {errorMessage} from '../utils/format'
export default function LoginPage(){const {user,login}=useAuth();const nav=useNavigate();const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [err,setErr]=useState('');if(user)return <Navigate to="/" replace/>
 const submit=async(e:FormEvent)=>{e.preventDefault();setErr('');if(!username||!password){setErr('نام کاربری و رمز عبور را وارد کنید.');return}setBusy(true);try{await login(username,password);nav('/')}catch(e){setErr(errorMessage(e))}finally{setBusy(false)}}
 return <div className="flex min-h-screen items-center justify-center bg-slate-100 p-4"><form onSubmit={submit} className="card w-full max-w-md p-6 sm:p-8"><div className="mb-6"><h1 className="text-2xl font-bold text-slate-900">ورود به سامانه</h1><p className="mt-2 text-sm text-slate-500">برای ادامه اطلاعات حساب کاربری خود را وارد کنید.</p></div>{err&&<div className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{err}</div>}<label className="mb-4 block"><span className="label">نام کاربری</span><input className="input" autoComplete="username" value={username} onChange={e=>setUsername(e.target.value)}/></label><label className="mb-6 block"><span className="label">رمز عبور</span><input className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn-primary w-full" disabled={busy}>{busy?'در حال ورود...':'ورود'}</button></form></div>}
